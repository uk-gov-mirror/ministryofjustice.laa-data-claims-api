openapi: "3.0.0"
info:
  version: 1.0.0
  title: LAA Data Claims API
  description: |
    This API allows bulk submission and claim management for the LAA Data Stewardship Payments project. 

tags:
  - name: Bulk Submissions
    description: Operations related to bulk submissions
  - name: Submissions
    description: Operations related to submissions
  - name: Claims
    description: Operations related to claims
  - name: Claim History
    description: Unified chronological history timeline for a single claim
  - name: Matter Starts
    description: Operations related to matter starts
  - name: System References
    description: Read-only governed reference data lookups (e.g. amendment metadata)

paths:
  /api/v1/bulk-submissions:
    post:
      operationId: createBulkSubmission
      tags:
        - Bulk Submissions
      summary: Upload a submission file
      description: >
        Upload a file (as multipart/form-data) containing data related to a submission.
        The file is converted into a JSON document and stored in the `bulk_submission` table.
        A 201 response is returned with a `Location` header containing the URI of the created record.
      parameters:
        - name: userId
          in: query
          required: true
          description: The user ID of the user performing the bulk submission
          schema:
            type: string
        - name: offices
          in: query
          schema:
            type: array
            items:
              type: string
            description: List of office account numbers
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: The submission file to be uploaded.
              required:
                - file
      responses:
        '201':
          description: Bulk Submission accepted and processed
          headers:
            Location:
              description: URI of the created bulk submission resource.
              schema:
                type: string
                format: uri
          content:
            application/json:
              schema:
                type: object
                description: Bulk Submission details
                properties:
                  bulk_submission_id:
                    type: string
                    format: uuid
                    description: UUID of the created bulk submission record.
                  submission_ids:
                    type: array
                    items:
                      type: string
                      format: uuid
                    description: list of UUIDs for the future submission records (only one submission per bulk expected), will have to be sent back in a later call.
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '415':
          description: 'Unsupported Media Type'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

  /api/v1/bulk-submissions/{id}:
    get:
      operationId: getBulkSubmission
      tags:
        - Bulk Submissions
      summary: Get the contents of a bulk submission
      description: |
        Returns the raw JSON document stored in the `bulk_submission` table,
        identified by its UUID.
      parameters:
        - name: id
          in: path
          required: true
          description: UUID of the bulk submission
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Successfully retrieved bulk submission resource
          content:
            application/json:
              schema:
                type: object
                description: The JSON object representing the `bulk_submission` stored in the corresponding table with bulk submission id and status
                properties:
                  bulk_submission_id:
                    type: string
                    format: uuid
                    description: UUID of the bulk submission.
                  status:
                    $ref: "#/components/schemas/bulk_submission_status"
                  created_by_user_id:
                    type: string
                    description: The user ID of the user who created the bulk submission.
                  error_code:
                    $ref: "#/components/schemas/bulk_submission_error_code"
                  error_description:
                    type: string
                  updated_by_user_id:
                    type: string
                  details:
                    type: object
                    description: The object storing all the details of the bulk submission
                    properties:
                      office:
                        type: object
                        description: The office submitting a claim
                        properties:
                          account:
                            type: string
                      schedule:
                        type: object
                        description: The details of the bulk submission schedule
                        properties:
                          submission_period:
                            type: string
                          area_of_law:
                            type: string
                          schedule_num:
                            type: string
                      outcomes:
                        type: array
                        description: List of outcome objects for the bulk submission
                        items:
                          $ref: '#/components/schemas/bulk_submission_outcome'
                      matter_starts:
                        type: array
                        description: List of matter start objects for the bulk submission
                        items:
                          $ref: '#/components/schemas/bulk_submission_matter_start'
                      immigration_clr:
                        type: array
                        description: List of immigration CLR rows captured from the bulk submission
                        items:
                          type: object
                          additionalProperties:
                            type: string

        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: Bulk submission not found
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

    patch:
      operationId: updateBulkSubmission
      tags:
        - Bulk Submissions
      summary: Update bulk submission status
      description: Updates the `status` field of an existing bulk submission identified by its ID.
      parameters:
        - name: id
          in: path
          required: true
          description: UUID of the bulk submission to update
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/bulk_submission_patch'
      responses:
        '204':
          description: 'Bulk submission status updated successfully'
          content: { }
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: 'Submission ID not found'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

  /api/v1/bulk-submissions/{id}/summary:
    get:
      operationId: getBulkSubmissionStatusById
      tags:
        - Bulk Submissions
      summary: Get the processing summary of a bulk submission
      description: Returns a minimal processing summary (currently only status) for the bulk submission identified by its UUID.
      parameters:
        - name: id
          in: path
          required: true
          description: UUID of the bulk submission
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Successfully retrieved bulk submission summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    $ref: "#/components/schemas/bulk_submission_status"
        '404':
          description: Bulk submission not found

  /api/v1/submissions:
    post:
      operationId: createSubmission
      tags:
        - Submissions
      summary: Create a new submission
      description: |
        Creates a new submission record associated with a bulk submission.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/submission_post'
      responses:
        '201':
          description: Submission created successfully.
          headers:
            location:
              description: URI of the created submission resource.
              schema:
                type: string
                format: uri
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                    description: UUID of the created submission.
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

    get:
      operationId: getSubmissions
      x-spring-paginated: true
      tags:
        - Submissions
      summary: get claim submissions
      description: Return claim submissions by office account numbers (mandatory), submissionId, submittedDateFrom, submittedDateTo, areaOfLaw, submissionPeriod
      parameters:
        - name: offices
          in: query
          required: true
          schema:
            type: array
            items:
              type: string
            description: Filter by list of office account numbers
        - name: submission_id
          in: query
          required: false
          schema:
            type: string
          description: Filter by submission ID
        - name: submitted_date_from
          in: query
          required: false
          schema:
            type: string
            format: date
          description: Filter submissions submitted from this date (inclusive)
        - name: submitted_date_to
          in: query
          required: false
          schema:
            type: string
            format: date
          description: Filter submissions submitted up to this date (inclusive)
        - name: area_of_law
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/area_of_law"
          description: Filter by area of law
        - name: submission_period
          in: query
          required: false
          schema:
            type: string
          description: Filter by submission period
        - name: submission_statuses
          in: query
          required: false
          schema:
            type: array
            items:
              $ref: "#/components/schemas/submission_status"
          description: Filter by submission statuses
        - name: sort
          in: query
          required: false
          schema:
            type: string
          description: |
            Sort results by a single field and direction, e.g. `createdOn,desc`.
            Supported fields: `createdOn`, `officeAccountNumber`, `areaOfLaw`, `submissionPeriod`, `status`.
            For createdOn and submissionPeriod, the sort is performed on the date value. For officeAccountNumber and areaOfLaw, the sort is performed in a case-insensitive manner.
            Where multiple rows share the same primary sort value, a deterministic secondary sort by `id` is applied.
            Null values sort last for ascending and first for descending (PostgreSQL default behaviour).
        - $ref: '#/components/parameters/pageParam'
        - $ref: '#/components/parameters/sizeParam'

      responses:
        '200':
          description: returns a list of paginated claim submissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/submissions_result_set'
        '429':
          description: 'Too many requests'

  /api/v2/claims:
    get:
      operationId: getClaimsV2
      x-spring-paginated: true
      tags:
        - Claims
      summary: Get claims
      description: Get claims for an office, filtering on certain fields.
      parameters:
        - name: office_code
          in: query
          required: true
          schema:
            type: string
          description: |
            The unique account number assigned to the provider office by the LAA.
        - name: submission_id
          in: query
          required: false
          schema:
            type: string
          description: |
            Unique system-generated identifier for each submission
        - name: submission_statuses
          in: query
          required: false
          schema:
            type: array
            items:
              $ref: "#/components/schemas/submission_status"
        - name: area_of_law
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/area_of_law"
          description: Filter by area of
        - name: fee_code
          in: query
          required: false
          schema:
            type: string
          description: |
            Alphanumeric code identifying the specific fee type or category applicable to the claim 
            under the relevant Legal Aid Agency fee scheme
        - name: unique_file_number
          in: query
          required: false
          schema:
            type: string
          description: Provider's unique case reference number for controlled work billing
        - name: unique_client_number
          in: query
          required: false
          schema:
            type: string
          description: Unique Client Number assigned to the client
        - name: unique_case_id
          in: query
          required: false
          schema:
            type: string
          description: Unique Case Id assigned to the claim case
        - name: claim_statuses
          in: query
          required: false
          schema:
            type: array
            items:
              $ref: "#/components/schemas/claim_status"
        - name: submission_period
          in: query
          required: false
          schema:
            type: string
          description: Filter by submission period
        - name: case_reference_number
          in: query
          required: false
          allowEmptyValue: false
          schema:
            type: string
          description: Filter by case reference number. On this filter a case-insensitive partial match that requires a minimum of 3 characters is performed.
        - name: escaped_case_flag
          in: query
          schema:
            type: boolean
          description: Filter by Escaped Case Flag
        - name: sort
          in: query
          required: false
          schema:
            type: string
          description: |
            Sort results by a field and direction, e.g. `status,asc` or `derived_claim_status,desc`.

            Supported fields include the plain claim columns exposed by the endpoint plus the
            computed fields `total_warnings`, `submission_period` and `derived_claim_status`.
            An unsupported field returns a 400 Problem Details response.

            `derived_claim_status` sorts by the canonical business ordering
            (ACCEPTED, AMENDED, ASSESSED, VOIDED, INVALID, READY_TO_PROCESS for ascending; the
            reverse for descending) rather than alphabetically. See the `derived_claim_status`
            schema and docs/derived-claim-status.md for details.

            A deterministic secondary sort by `id` (ascending, UUIDv7) is always appended as the
            final ordering clause so rows never drift between pages.

            Multi-field sort caveat: plain columns may be combined (each is applied in order,
            followed by the `id` tie-break). The computed fields (`total_warnings`,
            `submission_period`, `derived_claim_status`) are applied as single-field sorts and
            must NOT be combined with any other sort field in the same request; if combined the
            computed ordering is not guaranteed to be applied. See docs/derived-claim-status.md
            for how full computed multi-field sorting could be added in future.
        - $ref: '#/components/parameters/pageParam'
        - $ref: '#/components/parameters/sizeParam'
      responses:
        '200':
          description: Claims retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/claim_result_set_v2'
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

  /api/v1/claims:
    get:
      operationId: getClaims
      x-spring-paginated: true
      tags:
        - Claims
      summary: Get claims
      description: Get claims for an office, filtering on certain fields.
      parameters:
        - name: office_code
          in: query
          required: true
          schema:
            type: string
          description: |
            The unique account number assigned to the provider office by the LAA.
        - name: submission_id
          in: query
          required: false
          schema:
            type: string
          description: |
            Unique system-generated identifier for each submission
        - name: submission_statuses
          in: query
          required: false
          schema:
            type: array
            items:
              $ref: "#/components/schemas/submission_status"
        - name: fee_code
          in: query
          required: false
          schema:
            type: string
          description: |
            Alphanumeric code identifying the specific fee type or category applicable to the claim 
            under the relevant Legal Aid Agency fee scheme
        - name: unique_file_number
          in: query
          required: false
          schema:
            type: string
          description: Provider's unique case reference number for controlled work billing
        - name: unique_client_number
          in: query
          required: false
          schema:
            type: string
          description: Unique Client Number assigned to the client
        - name: unique_case_id
          in: query
          required: false
          schema:
            type: string
          description: Unique Case Id assigned to the claim case
        - name: claim_statuses
          in: query
          required: false
          schema:
            type: array
            items:
              $ref: "#/components/schemas/claim_status"
        - name: submission_period
          in: query
          required: false
          schema:
            type: string
          description: Filter by submission period
        - name: case_reference_number
          in: query
          required: false
          schema:
            type: string
          description: Filter by case reference number
        - name: sort
          in: query
          required: false
          schema:
            type: string
        - $ref: '#/components/parameters/pageParam'
        - $ref: '#/components/parameters/sizeParam'
      responses:
        '200':
          description: Claims retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/claim_result_set'
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'
  /api/v1/submissions/{id}/claims:
    post:
      operationId: createClaim
      tags:
        - Claims
      summary: Add a claim to a submission
      description: Adds a claim to the specified submission ID.
      parameters:
        - name: id
          in: path
          required: true
          description: UUID of the parent submission
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/claim_post'
      responses:
        '201':
          description: Claim created successfully
          headers:
            location:
              description: URI of the created claim resource
              schema:
                type: string
                format: uri
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                    description: UUID of the created claim
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '409':
          description: 'Conflict - a claim with the same line number already exists in this submission'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

  /api/v1/submissions/{id}/matter-starts:
    post:
      operationId: createMatterStart
      tags:
        - Matter Starts
      summary: Create a Matter Start for a Submission
      description: |
        Adds a Matter Start record to the submission with the specified ID.
        Returns HTTP 201 Created with a Location header pointing to the newly created resource.
      parameters:
        - name: id
          in: path
          required: true
          description: UUID of the submission
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/matter_start_post'
      responses:
        '201':
          description: Matter Start created successfully
          headers:
            location:
              description: URI of the newly created Matter Start
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                    description: UUID of the created Matter Start
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'
    get:
      operationId: getAllMatterStartsForSubmission
      tags:
        - Matter Starts
      summary: Returns all Matter Starts for a given Submission ID
      description: |
        Returns all Matter Start records belonging to the submission with the specified ID.
      parameters:
        - name: id
          in: path
          required: true
          description: The submission ID
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Returns all Matter Starts for a Submission ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/matter_start_result_set'
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: 'Submission ID not found'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'
  /api/v1/submissions/{id}/matter-starts/{matter-start-id}:
    get:
      operationId: getMatterStart
      tags:
        - Matter Starts
      summary: Returns a Matter Start for a Submission
      description: |
        Returns a Matter Start record belonging to the submission with the specified ID.
      parameters:
        - name: id
          in: path
          required: true
          description: UUID of the submission
          schema:
            type: string
            format: uuid
        - name: matter-start-id
          in: path
          required: true
          description: UUID of the matter start
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Returns a Matter Start for a Submission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/matter_start_get'
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

  /api/v1/submissions/{id}:
    patch:
      operationId: updateSubmission
      tags:
        - Submissions
      summary: Update submission status
      description: Updates the `status` field of an existing submission identified by its ID.
      parameters:
        - name: id
          in: path
          required: true
          description: UUID of the submission to update
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/submission_patch'
      responses:
        '204':
          description: 'Submission status updated successfully'
          content: {}
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: 'Submission ID not found'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

    get:
      operationId: getSubmission
      tags:
        - Submissions
      summary: Get a submission summary by ID
      description: Returns the submission details along with associated claims (ID and status only) and matter starts (IDs only).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: UUID of the submission
      responses:
        '200':
          description: Submission found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/submission_response'
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: 'Submission ID not found'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

  /api/v1/submissions/{submission-id}/claims/{claim-id}:
    get:
      operationId: getClaim
      tags:
        - Claims
      summary: Get a specific claim for a submission
      description: Returns the details of a specific claim associated with a submission.
      parameters:
        - name: submission-id
          in: path
          required: true
          description: ID of the submission
          schema:
            type: string
            format: uuid
        - name: claim-id
          in: path
          required: true
          description: ID of the claim
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Claim details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/claim_response'
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: Submission or claim not found
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

    patch:
      operationId: updateClaim
      tags:
        - Claims
      summary: Update fields of a specific claim
      description: Partially update a claim within a submission. Only fields provided in the request will be updated.
      parameters:
        - name: submission-id
          in: path
          required: true
          description: ID of the submission
          schema:
            type: string
            format: uuid
        - name: claim-id
          in: path
          required: true
          description: ID of the claim to update
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/claim_patch'
      responses:
        '204':
          description: Claim updated successfully
          content: {}
        '400':
          description: Invalid request
        '404':
          description: Submission or claim not found
        '429':
          description: 'Too many requests'

  /api/v2/submissions/{submission-id}/claims/{claim-id}:
    get:
      operationId: getClaimV2
      tags:
        - Claims
      summary: Get a specific claim for a submission
      description: Returns the details of a specific claim associated with a submission.
      parameters:
        - name: submission-id
          in: path
          required: true
          description: ID of the submission
          schema:
            type: string
            format: uuid
        - name: claim-id
          in: path
          required: true
          description: ID of the claim
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Claim details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/claim_response_v2'
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: Submission or claim not found
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'


  /api/v1/claims/{claim-id}/void:
    post:
      operationId: voidClaim
      tags:
        - Claims
      summary: Void a specific claim
      description: Voids a specific claim and creates a void assessment record.
      parameters:
        - name: claim-id
          in: path
          required: true
          description: ID of the claim
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/void_claim_request'
      responses:
        '201':
          description: Void assessment created successfully
          headers:
            location:
              description: URI of the newly created void assessment
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                required:
                  - id
                properties:
                  id:
                    type: string
                    format: uuid
                    description: UUID of the created void assessment
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: Submission or claim not found
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

  /api/v1/claims/{claimId}/history:
    get:
      operationId: getClaimHistory
      tags:
        - Claim History
      summary: Get a claim's unified history timeline
      description: |
        Returns the complete history of a single claim as one unified, chronologically ordered
        timeline (newest event first). The timeline is composed from multiple business tables
        (submission, amendments, assessments) in a single query and ordered purely by event time,
        with the UUIDv7 source id as a deterministic tie-breaker.

        **Call pattern (one-response):** history is retrieved with a single request by claim id.
        There are no split summary/detail endpoints, expansion flags or event-type/date-range
        filters; a consumer populates its history page from this one response, optionally bounding
        the page size with `limit`. Pagination is not offered yet (a future keyset/cursor overload
        may be added without reshaping this contract).

        **Default ordering:** `event_timestamp` descending (newest first), with the UUIDv7
        `source_id` descending as the deterministic same-timestamp tie-break. `event_type` never
        participates in ordering.

        **Actor fallback:** `actor_id` is always populated; where the source record holds no user
        id the agreed `SYSTEM` value is returned rather than the field being omitted.

        **Metadata extension point:** every event shares the common envelope and carries its
        event-specific attributes in the open `metadata` container, so later event types extend the
        contract without reshaping it.

        **Not found:** an unknown claim id returns `404`, consistent with existing claim reads.
      parameters:
        - name: claimId
          in: path
          required: true
          description: UUID of the claim
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          description: Maximum number of timeline events to return (page size).
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        '200':
          description: Claim history timeline retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/claim_history_result_set'
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: Claim not found
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'

  /api/v1/validation-messages:
    get:
      operationId: getValidationMessages
      x-spring-paginated: true
      tags:
        - Validation Messages
      summary: Gets all validation messages
      description: |
        Returns all the validation messages, optionally sorted by one of the supported fields.

        **Sortable fields** (supplied via the `sort` query parameter, e.g. `sort=client_surname,asc`):
        - `display_message`
        - `client_forename`
        - `client_surname`
        - `unique_client_number`
        - `client_2_forename`
        - `client_2_surname`
        - `client_2_ucn`
        - `unique_file_number`

        Sorting is **case-insensitive** (e.g. `"alice"` and `"Alice"` are treated as equal).

        **Null / blank handling:** rows whose sort-field value is `null` are placed **last** for
        ascending sorts and **first** for descending sorts (PostgreSQL default behaviour).
        This ensures incomplete records always appear at a predictable position.

        **Stable pagination:** when multiple rows share the same primary sort value the backend
        automatically appends a secondary sort by `id` in the same direction so that rows do not
        drift between pages.
      parameters:
        - name: submission-id
          in: query
          required: true
          description: ID of the submission
          schema:
            type: string
            format: uuid
        - name: claim-id
          in: query
          required: false
          description: ID of the claim
          schema:
            type: string
            format: uuid
        - name: type
          in: query
          required: false
          description: type of the validation message
          schema:
            $ref: '#/components/schemas/validation_message_type'
        - name: source
          in: query
          required: false
          description: where the validation message originated
          schema:
            type: string
        - name: sort
          in: query
          required: false
          schema:
            type: string
        - $ref: '#/components/parameters/pageParam'
        - $ref: '#/components/parameters/sizeParam'
      responses:
        '200':
          description: Validations messages found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationMessagesResponse'
        '429':
          description: 'Too many requests'

  /api/v1/claims/{claimId}/assessments:
    post:
      operationId: createAssessment
      tags:
        - Assessments
      summary: Create an Assessment for a Claim
      description: |
        Adds an Assessment record to the claim with the specified ID.
        Returns HTTP 201 Created with a Location header pointing to the newly created resource.
      parameters:
        - name: claimId
          in: path
          required: true
          description: UUID of the claim
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/assessment_post'
      responses:
        '201':
          description: Assessment created successfully
          headers:
            location:
              description: URI of the newly created Assessment
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                required:
                  - id
                properties:
                  id:
                    type: string
                    format: uuid
                    description: UUID of the created Assessment
        '400':
          description: 'Bad request'
        '401':
          description: 'Unauthorized'
        '403':
          description: 'Forbidden'
        '404':
          description: 'Claim ID or claim summary fee ID not found'
        '429':
          description: 'Too many requests'
        '500':
          description: 'Internal server error'
    get:
      operationId: getAssessments
      x-spring-paginated: true
      tags:
        - Assessments
      summary: Get all Assessments for a Claim
      description: |
        Retrieves a list of Assessment records associated with the specified Claim ID.
      parameters:
        - name: claimId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: sort
          in: query
          required: false
          schema:
            type: string
        - $ref: '#/components/parameters/pageParam'
        - $ref: '#/components/parameters/sizeParam'
      responses:
        '200':
          description: List of assessments retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/assessment_result_set'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Claim not found
        '429':
          description: 'Too many requests'
        '500':
          description: Internal server error

  /api/v1/claims/{claimId}/assessments/{assessmentId}:
    get:
      operationId: getAssessment
      tags:
        - Assessments
      summary: Get an Assessment for a Claim
      description: |
        Retrieves the Assessment record for the specified Claim and Assessment IDs.
      parameters:
        - name: claimId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: assessmentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Assessment retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/assessment_get'
        '404':
          description: Claim or Assessment not found
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '429':
          description: 'Too many requests'
        '500':
          description: Internal server error

  /api/v1/system/references/amendment-requested-by:
    get:
      operationId: getAmendmentRequestedByReferences
      tags:
        - System References
      summary: Get amendment Requested By reference data with nested Amendment Reasons
      description: |
        Returns the governed Requested By reference values and, for each one, the Amendment
        Reason values valid for that requesting party, in configured display order. Both active
        and inactive values are returned, each flagged with is_active, so consumers can resolve
        display labels for historical amendments while still identifying which values are
        currently selectable. The response is stable enough for consumers (e.g. AaBC) to cache
        once at startup and reuse for the lifetime of the running pod.
      responses:
        '200':
          description: Amendment reference data retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/amendment_requested_by_reference_list'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '429':
          description: 'Too many requests'
        '500':
          description: Internal server error

components:
  parameters:
    pageParam:
      name: page
      in: query
      required: false
      schema:
        type: integer
        minimum: 0
        default: 0
      description: Zero-based page index (0..N). Defaults to 0.
    sizeParam:
      name: size
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        default: 20
      description: Number of records per page. Defaults to 20.
  schemas:
    amendment_requested_by_reference_list:
      type: object
      description: Top-level wrapper for the amendment Requested By reference lookup.
      properties:
        requested_by:
          type: array
          description: >
            All Requested By values in display order (active and inactive), each with its reasons
            and an is_active flag.
          items:
            $ref: '#/components/schemas/amendment_requested_by_reference'
    amendment_requested_by_reference:
      type: object
      description: A governed Requested By reference value with its valid Amendment Reasons.
      properties:
        code:
          type: string
          description: Stable, machine-readable business code persisted on the amendment record.
        display_label:
          type: string
          description: Human-readable label for dropdown rendering.
        display_order:
          type: integer
          description: Lower values render first.
        is_active:
          type: boolean
          description: >
            Whether the value is currently selectable. Inactive values are still returned so
            consumers can resolve display labels for historical amendments.
        reasons:
          type: array
          description: >
            Amendment Reason values valid for this Requested By value, in display order (active and
            inactive, each flagged with is_active).
          items:
            $ref: '#/components/schemas/amendment_reason_reference'
    amendment_reason_reference:
      type: object
      description: A governed Amendment Reason reference value scoped to a Requested By value.
      properties:
        code:
          type: string
          description: Stable, machine-readable business code persisted on the amendment record.
        display_label:
          type: string
          description: Human-readable label for dropdown rendering.
        display_order:
          type: integer
          description: Lower values render first.
        is_active:
          type: boolean
          description: >
            Whether the reason is currently selectable. Inactive reasons are still returned so
            consumers can resolve display labels for historical amendments.
    bulk_submission_patch:
      type: object
      description: bulk submission patch details
      properties:
        bulk_submission_id:
          type: string
          format: uuid
          description: UUID of the bulk submission.
        status:
          $ref: "#/components/schemas/bulk_submission_status"
        error_code:
          $ref: "#/components/schemas/bulk_submission_error_code"
        error_description:
          type: string
        updated_by_user_id:
          type: string
    bulk_submission_status:
      type: string
      enum:
        - READY_FOR_PARSING
        - PARSING_COMPLETED
        - PARSING_FAILED
        - VALIDATION_FAILED
        - REPLACED
        - UNAUTHORISED
        - VALIDATION_SUCCEEDED
    bulk_submission_error_code:
      type: string
      enum:
        - P100 # Parsing error
        - V100 # Validation error
        - E100 # Other errors
    submission_post:
      allOf:
        - $ref: '#/components/schemas/submission_base'
      required:
        - submission_id
        - office_account_number
        - submission_period
        - area_of_law
        - status
        - provider_user_id
        - created_by_user_id
    submission_patch:
      allOf:
        - $ref: '#/components/schemas/submission_base'
      type: object
      properties:
        validation_messages:
          type: array
          items:
            $ref: '#/components/schemas/validation_message_patch'
    submission_status:
      type: string
      enum:
        - CREATED
        - READY_FOR_VALIDATION
        - VALIDATION_IN_PROGRESS
        - VALIDATION_SUCCEEDED
        - VALIDATION_FAILED
        - REPLACED
      description: |
        Processing status indicator tracking the lifecycle stage of the submission from receipt through to completion
    submission_claim:
      type: object
      description: Claim UUID and status
      properties:
        claim_id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/claim_status"
    submission_response:
      allOf:
        - $ref: '#/components/schemas/submission_base'
        - type: object
          properties:
            claims:
              type: array
              description: List of claims (ID + status) for this submission
              items:
                $ref: '#/components/schemas/submission_claim'
            matter_starts:
              type: array
              description: List of matter start IDs for this submission
              items:
                type: string
                format: uuid
    ValidationMessagesResponse:
      allOf:
        - $ref: "#/components/schemas/page"
      type: 'object'
      properties:
        content:
          type: 'array'
          items:
            $ref: '#/components/schemas/validation_message_base'
        total_claims:
          type: integer
          description: The total number of unique claims that have validation errors
    area_of_law:
      type: string
      enum:
        - CRIME LOWER
        - LEGAL HELP
        - MEDIATION
    submission_base:
      type: object
      description: submission details
      properties:
        submission_id:
          type: string
          format: uuid
          description: UUID for submission that was returned when bulk_submission was created.
        bulk_submission_id:
          type: string
          format: uuid
          description: Optional UUID of the associated bulk submission.
        office_account_number:
          type: string
          description: The office account number.
        submission_period:
          type: string
          description: Submission period (e.g., "JUL-2025").
        area_of_law:
          $ref: "#/components/schemas/area_of_law"
        provider_user_id:
          type: string
          description: The id of the provider user who created this submission (copied from bulk submission).
        status:
          $ref: "#/components/schemas/submission_status"
        crime_lower_schedule_number:
          type: string
          description: Optional crime lower schedule number.
        legal_help_submission_reference:
          type: string
          description: Optional legal help submission reference.
        mediation_submission_reference:
          type: string
          description: Optional mediation submission reference.
        previous_submission_id:
          type: string
          format: uuid
          description: UUID of the previous submission that this replaces, if any.
        is_nil_submission:
          type: boolean
          description: Whether the submission is a nil submission, i.e. a submission without any claims.
        number_of_claims:
          type: integer
          description: Number of claims in this submission.
        calculated_total_amount:
          type: number
          format: decimal
          description: Total amount calculated from the fee details of each claim.
          example: "0.00"
          nullable: true
        assessed_total_amount:
          type: number
          format: decimal
          description: Total amount calculated from the latest assessment for each claim.
          example: "0.00"
          nullable: true
        submitted:
          type: string
          format: date-time
          description: Date and time the submission was submitted
        created_by_user_id:
          type: string
          description: The id of the user who created the submission.
        error_messages:
          type: string
          description: Error messages associated with this submission.
    submissions_result_set:
      allOf:
        - $ref: "#/components/schemas/page"
      type: 'object'
      properties:
        content:
          type: 'array'
          items:
            $ref: '#/components/schemas/submission_base'

    claim_post:
      allOf:
        - $ref: '#/components/schemas/claim_base'
      required:
        - status
        - line_number
        - matter_type_code
        - created_by_user_id
    claim_patch:
      allOf:
        - $ref: '#/components/schemas/claim_response'
        - type: object
          properties:
            amendment_requested_by:
              type: string
              description: "Requesting party code for an amendment (governed reference). Use values from GET /api/v1/system/references/amendment-requested-by"
              nullable: true
            amendment_user_id:
              type: string
              format: uuid
              description: "UUID of the user who requested the amendment (provider/Entra user id)."
              nullable: true
            amendment_reason_code:
              type: string
              description: "Amendment reason code valid for the requesting party (see amendment_requested_by reference reasons)."
              nullable: true
            validation_messages:
              type: array
              items:
                $ref: '#/components/schemas/validation_message_patch'
    claim_status:
      type: string
      enum:
        - READY_TO_PROCESS
        - VALID
        - INVALID
        - VOID
      description: |
        System-maintained processing status indicator that tracks the validation lifecycle of a
        claim record from initial submission through to final approval or rejection.
    derived_claim_status:
      type: string
      enum:
        - ACCEPTED
        - AMENDED
        - ASSESSED
        - VOIDED
        - INVALID
        - READY_TO_PROCESS
      description: |
        Derived business status of a claim. This is a read-only value computed from the raw
        `claim_status` together with `has_assessment` and `is_amended`. It does NOT replace
        `claim_status`, which remains the authoritative processing status.

        The enum declaration order above is the canonical business ordering and is the single
        source of truth for both response mapping and sort ordering (ascending follows the order
        listed; descending is the reverse).

        Derivation precedence (first matching rule wins, not chronological):
          1. claim_status = VOID              -> VOIDED
          2. claim_status = INVALID           -> INVALID
          3. claim_status = READY_TO_PROCESS  -> READY_TO_PROCESS
          4. has_assessment = true            -> ASSESSED
          5. is_amended = true                -> AMENDED
          6. otherwise (claim_status = VALID) -> ACCEPTED

        A null `has_assessment` or `is_amended` is treated as false. `claim_status` is never null.
        See docs/derived-claim-status.md for the full specification and truth table.
    claim_result_set:
      allOf:
        - $ref: "#/components/schemas/page"
      type: 'object'
      properties:
        content:
          type: 'array'
          items:
            $ref: '#/components/schemas/claim_response'
    claim_result_set_v2:
      allOf:
        - $ref: "#/components/schemas/page"
      type: 'object'
      properties:
        content:
          type: 'array'
          items:
            $ref: '#/components/schemas/claim_response_v2'
    void_claim_request:
      type: object
      required:
        - created_by_user_id
        - assessment_reason
      properties:
        created_by_user_id:
          type: string
          format: uuid
        assessment_reason:
          type: string
    matter_start_result_set:
      type: 'object'
      properties:
        submission_id:
          type: string
          format: uuid
          description: UUID for submission.
        matter_starts:
          type: 'array'
          items:
            $ref: '#/components/schemas/matter_start_get'
    assessment_result_set:
      allOf:
        - $ref: "#/components/schemas/page"
      type: 'object'
      properties:
        assessments:
          type: 'array'
          items:
            $ref: '#/components/schemas/assessment_get'
    claim_base:
      type: object
      description: Claim details
      properties:
        id:
          type: string
        submission_id:
          type: string
        status:
          $ref: "#/components/schemas/claim_status"
        schedule_reference:
          type: string
        line_number:
          type: integer
        case_reference_number:
          type: string
        unique_file_number:
          type: string
        case_start_date:
          type: string
          description: Date the case was started (format DD/MM/YYYY)
        case_concluded_date:
          type: string
          description: Date the case was concluded (format DD/MM/YYYY)
        matter_type_code:
          type: string
        crime_matter_type_code:
          type: string
        fee_scheme_code:
          type: string
        fee_code:
          type: string
        procurement_area_code:
          type: string
        access_point_code:
          type: string
        delivery_location:
          type: string
        representation_order_date:
          type: string
          description: Date the rep order was created (format DD/MM/YYYY)
        suspects_defendants_count:
          type: integer
        police_station_court_attendances_count:
          type: integer
        police_station_court_prison_id:
          type: string
        dscc_number:
          type: string
        maat_id:
          type: string
        prison_law_prior_approval_number:
          type: string
        is_duty_solicitor:
          type: boolean
        is_youth_court:
          type: boolean
        scheme_id:
          type: string
        mediation_sessions_count:
          type: integer
        mediation_time_minutes:
          type: integer
        outreach_location:
          type: string
        referral_source:
          type: string
        client_forename:
          type: string
        client_surname:
          type: string
        client_date_of_birth:
          type: string
          description: Client's date of birth (format DD/MM/YYYY)
        unique_client_number:
          type: string
        client_postcode:
          type: string
        gender_code:
          type: string
        ethnicity_code:
          type: string
        disability_code:
          type: string
        is_legally_aided:
          type: boolean
        client_type_code:
          type: string
        home_office_client_number:
          type: string
        cla_reference_number:
          type: string
        cla_exemption_code:
          type: string
        client_2_forename:
          type: string
        client_2_surname:
          type: string
        client_2_date_of_birth:
          type: string
          description: Client 2's date of birth (format DD/MM/YYYY)
        client_2_ucn:
          type: string
        client_2_postcode:
          type: string
        client_2_gender_code:
          type: string
        client_2_ethnicity_code:
          type: string
        client_2_disability_code:
          type: string
        client_2_is_legally_aided:
          type: boolean
        case_id:
          type: string
        unique_case_id:
          type: string
        case_stage_code:
          type: string
        stage_reached_code:
          type: string
        standard_fee_category_code:
          type: string
        outcome_code:
          type: string
        designated_accredited_representative_code:
          type: string
        is_postal_application_accepted:
          type: boolean
        is_client_2_postal_application_accepted:
          type: boolean
        mental_health_tribunal_reference:
          type: string
        is_nrm_advice:
          type: boolean
        follow_on_work:
          type: string
        transfer_date:
          type: string
          description: Transfer Date (format DD/MM/YYYY)
        exemption_criteria_satisfied:
          type: string
        exceptional_case_funding_reference:
          type: string
        is_legacy_case:
          type: boolean
        advice_time:
          type: integer
        travel_time:
          type: integer
        waiting_time:
          type: integer
        net_profit_costs_amount:
          type: number
        net_disbursement_amount:
          type: number
        net_counsel_costs_amount:
          type: number
        disbursements_vat_amount:
          type: number
        travel_waiting_costs_amount:
          type: number
        net_waiting_costs_amount:
          type: number
        is_vat_applicable:
          type: boolean
        is_tolerance_applicable:
          type: boolean
        prior_authority_reference:
          type: string
        is_london_rate:
          type: boolean
        adjourned_hearing_fee_amount:
          type: integer
          description: "Note: actually stores the number of times the hearing was adjourned"
        is_additional_travel_payment:
          type: boolean
        costs_damages_recovered_amount:
          type: number
        meetings_attended_code:
          type: string
        detention_travel_waiting_costs_amount:
          type: number
        jr_form_filling_amount:
          type: number
        is_eligible_client:
          type: boolean
        court_location_code:
          type: string
        advice_type_code:
          type: string
        medical_reports_count:
          type: integer
        is_irc_surgery:
          type: boolean
        surgery_date:
          type: string
          description: Surgery Date (format DD/MM/YYYY)
        surgery_clients_count:
          type: integer
        surgery_matters_count:
          type: integer
        cmrh_oral_count:
          type: integer
        cmrh_telephone_count:
          type: integer
        ait_hearing_centre_code:
          type: string
        is_substantive_hearing:
          type: boolean
        ho_interview:
          type: integer
          description: "Number of Home Office Interviews"
        local_authority_number:
          type: string
        submission_period:
          type: string
          description: Submission period (e.g., "JUL-2025").
        created_by_user_id:
          type: string
          description: The id of the user who created the claim.
        is_amended:
          type: boolean
          description: Indicates if the claim has been amended.
        has_assessment:
          type: boolean
          description: Indicates if the claim has an associated assessment.
        version:
          type: integer
          format: int64
          description: Used for optimistic locking
    claim_response:
      allOf:
        - $ref: '#/components/schemas/claim_base'
        - type: object
          properties:
            total_warnings:
              type: integer
              description: Number of validation warnings for this claim.
            fee_calculation_response:
              $ref: '#/components/schemas/fee_calculation_patch'
    claim_response_v2:
      allOf:
        - $ref: '#/components/schemas/claim_base'
        - type: object
          properties:
            area_of_law:
              $ref: '#/components/schemas/area_of_law'
            office_code:
              type: string
              description: Account Office Code for this claim's submission.
            derived_claim_status:
              $ref: '#/components/schemas/derived_claim_status'
            total_warnings:
              type: integer
              description: Number of validation warnings for this claim.
            fee_calculation_response:
              $ref: '#/components/schemas/fee_calculation_patch'
            date_submitted:
              type: string
              format: date-time
              description: Date and time the submission was submitted
    fee_calculation_patch:
      type: object
      description: Fee calculation response from the Fee Scheme Platform
      properties:
        calculated_fee_detail_id:
          type: string
        claim_summary_fee_id:
          type: string
          format: uuid
        claim_id:
          type: string
          format: uuid
        fee_code:
          type: string
        fee_code_description:
          type: string
        fee_type:
          $ref: "#/components/schemas/fee_calculation_type"
        category_of_law:
          type: string
        total_amount:
          type: number
        vat_indicator:
          type: boolean
        vat_rate_applied:
          type: number
        calculated_vat_amount:
          type: number
        disbursement_amount:
          type: number
        requested_net_disbursement_amount:
          type: number
        disbursement_vat_amount:
          type: number
        hourly_total_amount:
          type: number
        fixed_fee_amount:
          type: number
        net_profit_costs_amount:
          type: number
        requested_net_profit_costs_amount:
          type: number
        net_cost_of_counsel_amount:
          type: number
        net_travel_costs_amount:
          type: number
        net_waiting_costs_amount:
          type: number
        detention_travel_and_waiting_costs_amount:
          type: number
        jr_form_filling_amount:
          type: number
        travel_and_waiting_costs_amount:
          type: number
        bolt_on_details:
          $ref: "#/components/schemas/bolt_on_patch"
    bolt_on_patch:
      type: object
      description: Bolt on details
      properties:
        bolt_on_total_fee_amount:
          type: number
        bolt_on_adjourned_hearing_count:
          type: integer
        bolt_on_adjourned_hearing_fee:
          type: number
        bolt_on_cmrh_telephone_count:
          type: integer
        bolt_on_cmrh_telephone_fee:
          type: number
        bolt_on_cmrh_oral_count:
          type: integer
        bolt_on_cmrh_oral_fee:
          type: number
        bolt_on_home_office_interview_count:
          type: integer
        bolt_on_home_office_interview_fee:
          type: number
        bolt_on_substantive_hearing_fee:
          type: number
        escape_case_flag:
          type: boolean
        scheme_id:
          type: string

    bulk_submission_matter_start:
      type: object
      description: matter start details for a bulk submission
      properties:
        schedule_ref:
          type: string
        category_code:
          $ref: '#/components/schemas/category_code'
        mediation_type:
          $ref: '#/components/schemas/mediation_type'
        procurement_area:
          type: string
        access_point:
          type: string
        delivery_location:
          type: string
        number_of_matter_starts:
          type: integer
    bulk_submission_outcome:
      type: object
      description: bulk submission outcome details
      properties:
        matter_type:
          type: string
        fee_code:
          type: string
        case_ref_number:
          type: string
        case_start_date:
          type: string
        case_id:
          type: string
        case_stage_level:
          type: string
        ufn:
          type: string
        procurement_area:
          type: string
        access_point:
          type: string
        client_forename:
          type: string
        client_surname:
          type: string
        client_date_of_birth:
          type: string
        ucn:
          type: string
        cla_ref_number:
          type: string
        cla_exemption:
          type: string
        gender:
          type: string
        ethnicity:
          type: string
        disability:
          type: string
        client_post_code:
          type: string
        work_concluded_date:
          type: string
        advice_time:
          type: integer
        travel_time:
          type: integer
        waiting_time:
          type: integer
        profit_cost:
          type: number
          format: bigDecimal
        value_of_costs:
          type: number
          format: bigDecimal
        disbursements_amount:
          type: number
          format: bigDecimal
        counsel_cost:
          type: number
          format: bigDecimal
        disbursements_vat:
          type: number
          format: bigDecimal
        travel_waiting_costs:
          type: number
          format: bigDecimal
        vat_indicator:
          type: boolean
        london_nonlondon_rate:
          type: boolean
        client_type:
          type: string
        tolerance_indicator:
          type: boolean
        travel_costs:
          type: number
          format: bigDecimal
        outcome_code:
          type: string
        legacy_case:
          type: boolean
        claim_type:
          type: string
        adjourned_hearing_fee:
          type: integer
          description: "Note: actually stores the number of times the hearing was adjourned"
        type_of_advice:
          type: string
        postal_appl_accp:
          type: boolean
        schedule_ref:
          type: string
        cmrh_oral:
          type: string
        cmrh_telephone:
          type: string
        ait_hearing_centre:
          type: string
        substantive_hearing:
          type: boolean
        ho_interview:
          type: integer
          description: "Number of Home Office Interviews"
        ho_ucn:
          type: string
        transfer_date:
          type: string
        detention_travel_waiting_costs:
          type: number
          format: bigDecimal
        delivery_location:
          type: string
        prior_authority_ref:
          type: string
        jr_form_filling:
          type: number
          format: bigDecimal
        additional_travel_payment:
          type: boolean
        meetings_attended:
          type: string
        medical_reports_claimed:
          type: integer
        desi_acc_rep:
          type: integer
          description: "designated accredited representative"
        mht_ref_number:
          type: string
        stage_reached:
          type: string
        follow_on_work:
          type: string
        national_ref_mechanism_advice:
          type: boolean
        exemption_criteria_satisfied:
          type: string
        excl_case_funding_ref:
          type: string
        no_of_clients:
          type: integer
        no_of_surgery_clients:
          type: integer
        irc_surgery:
          type: boolean
        surgery_date:
          type: string
        line_number:
          type: string
        crime_matter_type:
          type: string
        fee_scheme:
          type: string
        rep_order_date:
          type: string
        no_of_suspects:
          type: integer
        no_of_police_station:
          type: integer
        police_station:
          type: string
        dscc_number:
          type: string
        maat_id:
          type: string
        duty_solicitor:
          type: boolean
        youth_court:
          type: boolean
        scheme_id:
          type: string
        number_of_mediation_sessions:
          type: integer
        mediation_time:
          type: integer
        outreach:
          type: string
        referral:
          type: string
        client_legally_aided:
          type: boolean
        client2_forename:
          type: string
        client2_surname:
          type: string
        client2_date_of_birth:
          type: string
        client2_ucn:
          type: string
        client2_post_code:
          type: string
        client2_gender:
          type: string
        client2_ethnicity:
          type: string
        client2_disability:
          type: string
        client2_legally_aided:
          type: boolean
        unique_case_id:
          type: string
        standard_fee_cat:
          type: string
        client_2_postal_appl_accp:
          type: boolean
        costs_damages_recovered:
          type: number
          format: bigDecimal
        eligible_client:
          type: boolean
        court_location_hpcds:
          type: string
        local_authority_number:
          type: string
        pa_number:
          type: string
        excess_travel_costs:
          type: number
          format: bigDecimal
        med_concluded_date:
          type: string
    matter_start_post:
      allOf:
      - $ref: '#/components/schemas/matter_start_fields'
    matter_start_get:
      allOf:
      - $ref: '#/components/schemas/matter_start_fields'
    matter_start_fields:
      type: object
      description: Matter start for a submission
      properties:
        schedule_reference:
          type: string
        category_code:
          $ref: '#/components/schemas/category_code'
        procurement_area_code:
          type: string
        access_point_code:
          type: string
        delivery_location:
          type: string
        mediation_type:
          $ref: "#/components/schemas/mediation_type"
        number_of_matter_starts:
          type: integer
        created_by_user_id:
          type: string
          description: The id of the user who created the matter start.
    mediation_type:
      type: string
      enum:
        - "MDCS Child Only Sole"
        - "MDCC Child Only Co"
        - "MDPS Property & Finance Sole"
        - "MDPC Property & Finance Co"
        - "MDAS All Issues Sole"
        - "MDAC All Issues Co"
      description: |
        The type of mediation for matter start.
    category_code:
      type: string
      enum:
        - AAP
        - COM
        - CON
        - DEB
        - EDU
        - EMP
        - ELA
        - HOU
        - IMMAS
        - IMMOT
        - MAT
        - MED
        - MHE
        - MSC
        - PI
        - PUB
        - WB
        - DISC
      description: |
        Category code assigned to a legal aid matter start.
    validation_message_patch:
      type: object
      description: Validation message
      properties:
        type:
          $ref: "#/components/schemas/validation_message_type"
        source:
          type: string
        display_message:
          type: string
        technical_message:
          type: string
        message_code:
          type: string
          maxLength: 20
          description: Message code from FSP payload (e.g. WARFAM1, ERRALL1).
    validation_message_base:
      type: object
      description: Validation message
      properties:
        id:
          type: string
          format: uuid
        submission_id:
          type: string
          format: uuid
        claim_id:
          type: string
          format: uuid
        type:
          $ref: "#/components/schemas/validation_message_type"
        source:
          type: string
        display_message:
          type: string
        message_code:
          type: string
          maxLength: 20
          description: Message code from FSP payload.
        client_forename:
          type: string
          description: Client 1 forename from the associated claim
        client_surname:
          type: string
          description: Client 1 surname from the associated claim
        unique_client_number:
          type: string
          description: Client 1 Unique Client Number (UCN) from the associated claim
        client_2_forename:
          type: string
          description: Client 2 forename from the associated claim
        client_2_surname:
          type: string
          description: Client 2 surname from the associated claim
        client_2_ucn:
          type: string
          description: Client 2 Unique Client Number (UCN) from the associated claim
        unique_file_number:
          type: string
          description: Unique File Number (UFN) from the associated claim
    validation_message_type:
      type: string
      enum:
        - ERROR
        - WARNING
      description: |
        The type of validation message, indicating its severity or nature.
    fee_calculation_type:
      type: string
      enum:
        - HOURLY
        - FIXED
        - DISB_ONLY
      description: |
        The type of the fee, indicating the nature of the calculated fee returned by Fee Scheme Platform.
    assessment_outcome:
      type: string
      enum:
        - PAID_IN_FULL
        - REDUCED_STILL_ESCAPED
        - REDUCED_TO_FIXED_FEE
        - NILLED
    assessment_type:
      type: string
      enum:
        - ESCAPE_CASE_ASSESSMENT
        - STAGE_DISBURSEMENT_ASSESSMENT
        - VOID
      description: |
        The type of assessment being performed on a claim.
        - ESCAPE_CASE_ASSESSMENT: Assessment for claims that have escaped the fixed fee threshold.
        - STAGE_DISBURSEMENT_ASSESSMENT: Assessment for stage disbursement claims.
        - VOID: Used when voiding a previously submitted assessment.
    assessment_base:
      type: object
      properties:
        id:
          type: string
          format: uuid
        claim_id:
          type: string
          format: uuid
        claim_summary_fee_id:
          type: string
          format: uuid
        assessment_outcome:
          $ref: "#/components/schemas/assessment_outcome"
        assessment_reason:
          type: string
        fixed_fee_amount:
          type: number
        net_profit_costs_amount:
          type: number
        disbursement_amount:
          type: number
        disbursement_vat_amount:
          type: number
        net_cost_of_counsel_amount:
          type: number
        net_travel_costs_amount:
          type: number
        net_waiting_costs_amount:
          type: number
        detention_travel_and_waiting_costs_amount:
          type: number
        jr_form_filling_amount:
          type: number
        bolt_on_adjourned_hearing_fee:
          type: number
        bolt_on_cmrh_telephone_fee:
          type: number
        bolt_on_cmrh_oral_fee:
          type: number
        bolt_on_home_office_interview_fee:
          type: number
        bolt_on_substantive_hearing_fee:
          type: number
        is_vat_applicable:
          type: boolean
        created_by_user_id:
          type: string
        assessed_total_vat:
          type: number
        assessed_total_incl_vat:
          type: number
        allowed_total_vat:
          type: number
        allowed_total_incl_vat:
          type: number
        assessment_type:
          $ref: "#/components/schemas/assessment_type"
    assessment_post:
      allOf:
        - $ref: '#/components/schemas/assessment_base'
      required:
        - claim_id
        - claim_summary_fee_id
        - assessment_outcome
        - created_by_user_id
        - assessed_total_vat
        - assessed_total_incl_vat
        - allowed_total_vat
        - allowed_total_incl_vat
    assessment_get:
      allOf:
        - $ref: '#/components/schemas/assessment_base'
      properties:
        created_on:
          type: string
          format: date-time
          description: Date and time the assessment was created
    claim_history_event_type:
      type: string
      enum:
        - SUBMISSION
        - AMENDMENT
        - ASSESSMENT
        - VOID
      description: |
        Discriminator identifying which business source produced the timeline event.
    claim_history_event:
      type: object
      description: A single entry on a claim's history timeline.
      properties:
        event_type:
          $ref: '#/components/schemas/claim_history_event_type'
        event_timestamp:
          type: string
          format: date-time
          description: Business time the event occurred; the sole driver of timeline ordering.
        actor_id:
          type: string
          description: >
            The user who caused the event. Always populated; "SYSTEM" when the source record has no
            user id.
        source_id:
          type: string
          format: uuid
          description: >
            Primary key (UUIDv7) of the originating row, used as the deterministic ordering
            tie-breaker and future keyset cursor.
        metadata:
          type: object
          additionalProperties: true
          description: >
            Event-specific attributes, shaped per event type. The concrete shape is determined by
            event_type; see the documented per-type schemas:
            claim_history_submission_metadata (SUBMISSION),
            claim_history_amendment_metadata (AMENDMENT),
            claim_history_assessment_metadata (ASSESSMENT) and
            claim_history_void_metadata (VOID). The container stays open (additionalProperties)
            so new event types and attributes can be added without reshaping the contract.
    claim_history_change_entry:
      type: object
      description: >
        A single field-level change recorded on an amendment, lifted verbatim from the persisted
        amendment diff. change_source distinguishes provider-requested changes from FSP
        (Fee Scheme Platform) repricing consequences.
      properties:
        field_identifier:
          type: string
          description: Stable, machine-readable identifier of the changed field (not a display label).
        change_source:
          type: string
          enum:
            - REQUESTED
            - FSP
          description: >
            Origin of the change. "REQUESTED" is a provider-requested change; "FSP" is a downstream
            consequence of Fee Scheme Platform repricing. The value is the stable, upper-case label
            persisted in the amendment diff and surfaced verbatim.
        before:
          nullable: true
          description: Value before the amendment, in its natural JSON type. Null means cleared.
        after:
          nullable: true
          description: Value after the amendment, in its natural JSON type. Null means cleared.
    claim_history_submission_metadata:
      type: object
      description: Metadata carried by a SUBMISSION event.
      properties:
        submission_period:
          type: string
          description: The submission period the claim belongs to.
        office_account_number:
          type: string
          description: The provider office account number for the submission.
        area_of_law:
          type: string
          description: The area of law of the submission.
    claim_history_amendment_metadata:
      type: object
      description: >
        Metadata carried by an AMENDMENT event. The FSP indicators are derived exclusively from
        amendment-linked persisted data (the linked calculated_fee_detail and the amendment diff),
        never inferred from current claim state.
      properties:
        requested_by_code:
          type: string
          description: Governed reference code for the requesting party (e.g. PROVIDER).
        amendment_reason_code:
          type: string
          description: Governed reference code for the amendment reason.
        pricing_recalculated:
          type: boolean
          description: >
            True when FSP repricing ran for this amendment, i.e. a calculated_fee_detail row is
            linked to the amendment.
        price_changed:
          type: boolean
          description: >
            True when repricing changed the monetary value (currently driven by a change in
            total_amount versus the previous calculated_fee_detail). False when repricing ran with
            no monetary change, or when it did not run.
        escape_case_logged:
          type: boolean
          description: >
            True only when this amendment CAUSED the claim to become an escape case
            (an FSP-sourced fee.escapeCaseFlag transition from false to true recorded in the
            amendment diff). An already-escaped claim, or an amendment that did not trigger an
            escape transition, yields false.
        changes:
          type: array
          description: Field-level changes recorded on this amendment (may be empty).
          items:
            $ref: '#/components/schemas/claim_history_change_entry'
    claim_history_assessment_metadata:
      type: object
      description: Metadata carried by an ASSESSMENT event.
      properties:
        assessment_type:
          type: string
          nullable: true
          description: The type of assessment (null for legacy rows with no populated type).
        assessment_outcome:
          type: string
          nullable: true
          description: The assessment outcome (null when not yet determined).
        assessment_reason:
          type: string
          description: The reason recorded against the assessment.
    claim_history_void_metadata:
      type: object
      description: >
        Metadata carried by a VOID event. A void carries no outcome; assessment_reason holds the
        void reason.
      properties:
        assessment_type:
          type: string
          description: Always VOID for this event type.
        assessment_reason:
          type: string
          description: The reason the claim was voided.
    claim_history_result_set:
      type: object
      description: A claim's history events in reverse-chronological order.
      properties:
        claim_id:
          type: string
          format: uuid
          description: The claim the timeline belongs to.
        events:
          type: array
          description: Timeline events, newest first.
          items:
            $ref: '#/components/schemas/claim_history_event'
    page:
      type: 'object'
      properties:
        total_pages:
          type: 'integer'
        total_elements:
          type: 'integer'
        number:
          type: 'integer'
        size:
          type: 'integer'
